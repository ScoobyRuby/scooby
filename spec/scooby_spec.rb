# frozen_string_literal: true

RSpec.describe Scooby do
  it "has a version number" do
    expect(Scooby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to_not eq(true)
  end
end
